'use client';

// Template default; copied by scaffold. Replace the suffix when creating a new region.
if (typeof globalThis !== 'undefined') {
  if (!(globalThis as any).__ART_NOTIFICATIONS_STORAGE_KEY) {
    (globalThis as any).__ART_NOTIFICATIONS_STORAGE_KEY = 'notifications-store:region-template';
  }
}
